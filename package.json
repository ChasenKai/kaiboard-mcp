{
  "name": "kaiboard-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "KaiBoard Model Context Protocol monorepo — @kaiboard/core (storage-agnostic Agent co-drawing command core) + @kaiboard/mcp-server (stdio JSON-RPC MCP server). Lets AI agents draw on local-first KaiBoard whiteboards.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc -p packages/core/tsconfig.json --noEmit",
    "typecheck:core": "tsc -p packages/core/tsconfig.json --noEmit",
    "build": "npm run build -w @kaiboard/core && npm run build -w @kaiboard/mcp-server",
    "build:core": "npm run build -w @kaiboard/core",
    "e2e": "node packages/server/test/e2e.mjs",
    "smoke": "node packages/server/test/mcp-client-smoke.mjs"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
